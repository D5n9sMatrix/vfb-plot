' The salary the print the plot about match of bic to father of family features
' reajust of law to transposition about day util of submit to templates dark
' changes promise to compry report message propost of action to peoples
' business money to population.
Declare Sub Plot (ByRef Salary As Double)

#if  __fb_fpmode__ = "precise"
Type Salary
     Dim SalaryTypes As Double
     Dim SalaryEvent As Double
     Dim SalaryVals As Double 
End Type 
#else

Print "the ascii code of 'a' is:"; Asc("a")
Print "the ascii code of 'b' is:"; Asc("abc", 2)

#endif
End

