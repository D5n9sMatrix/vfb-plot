' The case very comfirm to variables of mono scientific
' Sientistics of relation of covid-19 to comfirm the relation
' Srever ware relaxed method to variables about vacines
' Report message.
Declare Sub ReportMessage ( ByRef  Vacines As Double)

#if  __fb_mt__
Type Vacines
       Dim VacinesTypes As Double
       Dim VacinesEvent As Double
       Dim VacinesObject As Double
End Type       
#else

Dim n As Double, e As String
n = 1.2345
e = MKD(n)
Print n, CVD(e)

#endif

End

